{
  "name": "pi-extension-dictate",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "description": "Minimal local voice dictation for pi (whisper.cpp, offline, no audio files written)",
  "license": "MIT",
  "keywords": ["pi-package"],
  "pi": {
    "extensions": ["./index.ts"]
  },
  "scripts": {
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "0.80.10",
    "@earendil-works/pi-tui": "0.80.10",
    "@types/node": "^24.0.0",
    "typescript": "^5.5.0"
  }
}
